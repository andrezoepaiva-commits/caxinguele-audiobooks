# Arquivo de exemplo para variáveis de ambiente
# Copie para .env e preencha com seus valores reais

# GitHub PAT (Personal Access Token) para publicar RSS
# 1. Vá para https://github.com/settings/tokens
# 2. Clique "Generate new token"
# 3. Marque: repo (apenas repo, não precisa more)
# 4. Copie o token e cole aqui
GITHUB_TOKEN=seu_token_aqui

# Nota: Este arquivo é um template apenas para documentação
# Nunca commit arquivo .env com token real no git!
# Use .gitignore para proteger: .env, token.json, credentials.json
